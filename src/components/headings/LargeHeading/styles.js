import {StyleSheet} from 'react-native';
import {OPEN_SANS_BOLD, FONT_SIZE_XL} from '../../../config/Constants';

// Exporting style
export default StyleSheet.create({
  style: {
    fontFamily: OPEN_SANS_BOLD,
    fontSize: FONT_SIZE_XL,
  },
});
